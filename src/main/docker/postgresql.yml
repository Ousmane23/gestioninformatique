version: '2'
services:
  gestioninformatique-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/gestioninformatique/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=gestioninformatique
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
